## Änderung
Den Arbeitsvertrag von **Melanie Schier** korrigieren, damit der Name nicht mehr doppelt erscheint.

## Umsetzung
- Den eindeutig gefundenen Vertrag `7ea11352-09f5-475f-aaba-b609c8e490d3` aktualisieren.
- `first_name` auf `Melanie` und `last_name` auf `Schier` setzen.
- Anschließend den Datensatz erneut abfragen und die gespeicherte Namensaufteilung verifizieren.

## Hinweis
Es werden ausschließlich die beiden Namensfelder dieses Vertrags geändert; alle übrigen Vertragsdaten bleiben unverändert.